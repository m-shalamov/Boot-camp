1) Для задания, которое мы делали в классе с созданием api для получения координат города, реализовать сохранение города и координат в базу данных.
2) Для всех городов Канады (https://en.wikipedia.org/wiki/List_of_cities_in_Canada) протестировать api, например, с помощью библиотеки requests.
 Т.е. мы будем обращаться к нашей api для каждого города из Канады и результаты записывать в БД. 
 Внутри себя api может обращаться к разичным сервисам для получения данных. Таким образом, получается, что наша api будет прослойкой.
3) Сформировать отчет о проделанной работе